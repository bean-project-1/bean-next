import { ScheduleView } from '@/features/schedule/ScheduleView';

export const metadata = {
  title: 'Calendario — BEAN',
  description: 'Rastrea tus metas y actividades en el tiempo.',
};

export default function SchedulePage() {
  return <ScheduleView />;
}
