import { CommitmentSettings } from '@/features/profile/CommitmentSettings';

export default function SettingsPage() {
  return (
    <main className="min-h-screen bg-slate-50/30 py-12">
      <CommitmentSettings />
    </main>
  );
}
